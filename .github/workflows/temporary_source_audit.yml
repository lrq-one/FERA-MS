name: Temporary source-maintenance runner
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary source cleanup is being prepared; no repository files are modified.'
